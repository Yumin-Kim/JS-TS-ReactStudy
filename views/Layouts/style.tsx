import styled from "styled-components";
import Layout from "antd/lib/layout";

const { Header } = Layout;

export const Custom_Header = styled(Header)`
  background-color: rgba(0, 0, 0, 0.12);
  bos-shadow: 3px 5px 5px rgba(0, 0, 0, 0.5);
`;
